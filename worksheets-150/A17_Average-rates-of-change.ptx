

<worksheet xml:id="A17_Average-rates-of-change">
  <title>Math 150  Activity 17: Average rates of change</title>
  <p>
    \sisetup{group-separator={{,}}, group-minimum-digits={3}}
  </p>
  <exercise workspace="10%">
    <statement>
      <p>
        Define the average rate of change of a function between <m>x = a</m> and <m>x = b</m>.
      </p>
    </statement>
  </exercise>
  <exercise workspace="10%">
    <statement>
      <p>
        Find the average rate of change of
        <m>f(x) = 2x^2</m> between <m>x = 1</m> and <m>x = 3</m>.
      </p>
    </statement>
  </exercise>
  <exercise workspace="10%">
    <statement>
      <p>
        The table below gives values of a function <m>w = g(t)</m>.
        Is this function increasing or decreasing?
        Find the average rate of change of <m>w</m> between <m>t = 0</m> and <m>t = 18</m>.
      </p>

      <p>
        <m>\begin{array}{c||r|r|r|r|r|r|r|r|r|r} t \amp 0 \amp 2 \amp 4 \amp 6 \amp 8 \amp 10 \amp 12 \amp 14 \amp 16 \amp 18 \\ \hline w \amp 14 \amp 13 \amp 11 \amp 9 \amp 8 \amp 7 \amp 6 \amp 4 \amp 3 \amp 2 \end{array}</m>
      </p>
    </statement>
  </exercise>
  <exercise>
    <statement>
      <p>
        The graph of <m>y = h(t)</m> is shown below.
      </p>
      <image>
        <latex-image>
          <![CDATA[\begin{tikzpicture}\begin{axis}[
          axis x line=middle,
          axis y line=middle,
          xlabel=\(t\),
          ylabel=\(y\),
          minor x tick num = 1,
          minor y tick num = 0,
          samples = 40,
          grid = both,
          width = \linewidth,
          plot box ratio = 1,
          enlargelimits=false
          ]
          \addplot[domain=0:9, black, ultra thick] {2.3*sqrt(x)};
          \end{axis}\end{tikzpicture}]]>
        </latex-image>
      </image>

      <task workspace="10%">
        <p>
          Estimate the average rate of change of <m>h(t)</m> between <m>t = 1</m> and <m>t = 7</m>.
        </p>
      </task>

      <task workspace="10%">
        <p>
          Estimate the average rate of change of <m>h(t)</m> between <m>t = 2</m> and <m>t = 6</m>.
        </p>
      </task>

      <task workspace="10%">
        <p>
          Estimate the average rate of change of <m>h(t)</m> between <m>t = 3</m> and <m>t = 5</m>.
        </p>
      </task>

    </statement>
  </exercise>
  <exercise workspace="20%">
    <statement>
      <p>
        What do you think is the approximate average rate of change of <m>h(t)</m> between
        <m>t = 3.999</m> and <m>t = 4.001</m>?
      </p>
    </statement>
  </exercise>

  <pagebreak />

  <exercise>
    <statement>
      <p>
        The word <em>secant</em>
        is from a Latin word meaning <q>to cut</q>.
        Secant lines cut the curve.
        Estimate the slopes of the secant lines cutting the curve at the indicated <m>x</m>-values.
      </p>
      <image>
        <latex-image>
          <![CDATA[\begin{tikzpicture}[xscale=1,yscale=1]
          \begin{axis}[
          axis x line=middle,
          axis y line=middle,
          ymin=0,
          xlabel=\(x\),
          ylabel=\(y\),
          minor x tick num = 1,
          minor y tick num = 1,
          xmin = 0, xmax = 3,
          ymin = 0, ymax = 2.5,
          samples = 50,
          grid = both,
          enlargelimits=false,
          width = \linewidth,
          plot box ratio = 1
          ]
          \addplot[domain=0:4, black, ultra thick] {-(1/3)*(x*(x-5))};
          \end{axis}\end{tikzpicture}]]>
        </latex-image>
      </image>

      <task workspace="10%">
        <p>
          <m>x = 1</m> and <m>x = 3</m>
        </p>
      </task>

      <task workspace="10%">
        <p>
          <m>x = 1</m> and <m>x = 2</m>
        </p>
      </task>

      <task workspace="10%">
        <p>
          <m>x = 1</m> and <m>x = 1.5</m>
        </p>
      </task>

      <task workspace="10%">
        <p>
          <m>x = 1</m> and <m>x = 1.1</m>
        </p>
      </task>

    </statement>
  </exercise>
  <exercise workspace="10%">
    <statement>
      <p>
        Approximate the slope of the tangent line to the graph above at <m>x = 1</m>,
        and explain why you selected that value.
      </p>
    </statement>
  </exercise>
  <exercise>
    <statement>
      <p>
        The graph of <m>y = f(x)</m> is shown below.
      </p>
      <image>
        <latex-image>
          <![CDATA[\begin{tikzpicture}\begin{axis}[
          axis x line=middle,
          axis y line=middle,
          ymin=0,
          xlabel=\(x\),
          ylabel=\(y\),
          minor x tick num = 1,
          minor y tick num = 1,
          samples = 50,
          grid = both,
          enlargelimits=false,
          width = 0.9\linewidth,
          plot box ratio = 1
          ]
          \addplot[domain=0:9, black, ultra thick] {0.1*(x - 1.4)*(x - 4)*(x - 8)+5};
          \end{axis}\end{tikzpicture}]]>
        </latex-image>
      </image>

      <task workspace="10%">
        <p>
          Estimate <m>f'(2)</m>.
        </p>
      </task>

      <task workspace="10%">
        <p>
          Estimate <m>f'(4)</m>.
        </p>
      </task>

      <task workspace="10%">
        <p>
          Estimate <m>f'(6)</m>.
        </p>
      </task>

    </statement>
  </exercise>
</worksheet>

