

<worksheet xml:id="A12_Vectors-and-systems-of-Linear-Equations">
  <title>Math 150  Activity 12: Vectors and systems of linear equations</title>
  <exercise>
    <statement>
      <p>
        Use the following grid for this problem:
      </p>

      <p>
        <image> <latex-image> <![CDATA[\begin{tikzpicture}[xscale=0.65,yscale=0.65] \draw [style={gray,
        thick}] (0,0) grid (12,12); \draw [\lt ->, style={black,
        very thick}] (0,6) -- (12,6); \draw [\lt ->, style={black,
        very thick}] (6,0) -- (6,12); \end{tikzpicture}]]> </latex-image> </image>
      </p>

      <task workspace="10%">
        <p>
          Sketch: <m>\vec{u} = \begin{bmatrix}1 \\ 2 \end{bmatrix}</m> and <m>\vec{v} = \begin{bmatrix}3 \\ 1 \end{bmatrix}</m>.
        </p>
      </task>

      <task workspace="20%">
        <p>
          Compute and sketch: <m>-3 \vec{u}</m> and <m>2 \vec{v}</m>.
        </p>
      </task>

      <task workspace="20%">
        <p>
          Compute and sketch: <m>-3 \vec{u} + 2 \vec{v}</m>
        </p>

        <p>
          (this is called a <em>linear combination</em>
          of <m>\vec{u}</m> and <m>\vec{v}</m>)
        </p>
      </task>

      <task>
        <p>
          Sketch the parallelogram associated to part (c).
        </p>
      </task>

    </statement>
  </exercise>
  <exercise>
    <statement>
      <p>
        Use the following grid for this problem:
      </p>

      <p>
        <image> <latex-image> <![CDATA[\begin{tikzpicture}[xscale=0.65,yscale=0.65] \draw [style={gray,
        thick}] (0,0) grid (12,12); \draw [\lt ->, style={black,
        very thick}] (0,6) -- (12,6); \draw [\lt ->, style={black,
        very thick}] (6,0) -- (6,12); \end{tikzpicture}]]> </latex-image> </image>
      </p>

      <task>
        <p>
          Sketch the vectors <m>\begin{bmatrix}2 \\ 2 \end{bmatrix}</m>,
          <m>\begin{bmatrix}-1 \\ 3 \end{bmatrix}</m>,
          and <m>\begin{bmatrix}5 \\ 1 \end{bmatrix}</m>.
        </p>
      </task>

      <task>
        <p>
          Draw a parallelogram to estimate numbers <m>x</m> and <m>y</m> which satisfy the following
          <em>vector equation:</em>
          <me>
            x\begin{bmatrix}2 \\ 2
            \end{bmatrix} +y\begin{bmatrix}-1 \\ 3
            \end{bmatrix} =\begin{bmatrix}5 \\ 1
            \end{bmatrix}
          </me>
        </p>
      </task>

      <task workspace="20%">
        <p>
          The vector equation in part (b) corresponds the the following system of linear equations:
          <me>
            \begin{array}{l}
            2x-y=5 \\
            2x+3y=1
            \end{array}
          </me>
        </p>

        <p>
          Solve this system for <m>x</m> and <m>y</m> algebraically to check your answer for part (b).
        </p>
      </task>

    </statement>
  </exercise>

  <pagebreak />

  <exercise>
    <statement>
      <p>
        Use the following grid for this problem:
      </p>

      <p>
        <image> <latex-image> <![CDATA[\begin{tikzpicture}[xscale=0.65,yscale=0.65] \draw [style={gray,
        thick}] (0,0) grid (12,12); \draw [\lt ->, style={black,
        very thick}] (0,6) -- (12,6); \draw [\lt ->, style={black,
        very thick}] (6,0) -- (6,12); \end{tikzpicture}]]> </latex-image> </image>
      </p>

      <task>
        <p>
          Sketch the vectors <m>\begin{bmatrix}1 \\ 1 \end{bmatrix}</m>,
          <m>\begin{bmatrix}3 \\ -3 \end{bmatrix}</m>,
          and <m>\begin{bmatrix}0 \\ -6 \end{bmatrix}</m>.
        </p>
      </task>

      <task>
        <p>
          Draw a parallelogram to estimate numbers <m>x</m> and <m>y</m> which satisfy the vector equation:
          <me>
            x\begin{bmatrix}1 \\ 1
            \end{bmatrix} +y\begin{bmatrix}3 \\ -3
            \end{bmatrix} =\begin{bmatrix}0 \\ -6
            \end{bmatrix}
          </me>
        </p>
      </task>

      <task>
        <p>
          Write down the system of linear equations that correspond to the vector equation in part (b).
        </p>
      </task>

      <task>
        <p>
          Solve the system you found in part (c) and check that it agrees with your answer for part (b).
        </p>
      </task>

    </statement>
  </exercise>

  <sidebyside>
    <exercise>
      <statement>
        <p>
          Convert the given system of linear equations to vector form.
        </p>

        <task workspace="20%">
          <md>
            <mrow>5x + 7y \amp = 3</mrow>
            <mrow>2x - y \amp = 0</mrow>
          </md>
        </task>

        <task workspace="20%">
          <md>
            <mrow>2x + 3y + 4z \amp = 5</mrow>
            <mrow>-x - 2y - 6z \amp = 0</mrow>
          </md>
        </task>

        <task workspace="20%">
          <md>
            <mrow>x + y - z - w \amp = 4</mrow>
            <mrow>3x - 2y + 6z + w \amp = 0</mrow>
            <mrow>-3y - 2x -7w \amp = 2</mrow>
          </md>
        </task>

      </statement>
    </exercise>
  </sidebyside>
</worksheet>

